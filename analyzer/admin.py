# Admin interface (optional, not used for core functionality)
from django.contrib import admin

# Register your models here.
# Note: This application is stateless and doesn't use Django models for user data
